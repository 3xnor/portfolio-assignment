<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>지우의 포트폴리오</title>
    
    <link rel="stylesheet" href="/css/style.css">
    
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;700&display=swap" rel="stylesheet">
    
    <style>
        /* 폰트 적용을 위한 추가 스타일 */
        body { font-family: 'Noto Sans KR', sans-serif; }
    </style>
</head>
<body>

    <div class="container">
        <section class="profile-section">
            <% if (profile) { %>
                <h1>안녕하세요, 개발자 <span style="color:#3498db"><%= profile.name %></span>입니다.</h1>
                
                <p class="intro"><%= profile.intro %></p>
                
                <p class="email">
                    CONTACT: <a href="mailto:<%= profile.email %>" style="color:#7f8c8d; text-decoration:none;"><%= profile.email %></a>
                </p>
            <% } else { %>
                <h1>데이터를 불러오지 못했습니다.</h1>
                <p>DB 연결 설정을 확인해주세요!</p>
            <% } %>
        </section>

        <section>
            <h2 class="section-title">My Projects</h2>
            
            <div class="project-grid">
                <% if (projects.length > 0) { %>
                    <% projects.forEach(function(project) { %>
                        
                        <div class="project-card">
                            <img src="<%= project.img_url ? project.img_url : 'https://via.placeholder.com/300x200?text=No+Image' %>" alt="<%= project.title %>">
                            
                            <div class="card-content">
                                <h3><%= project.title %></h3>
                                
                                <p><%= project.description %></p>
                                
                                <a href="<%= project.link_url %>" target="_blank" class="btn">
                                    View Code &rarr;
                                </a>
                            </div>
                        </div>

                    <% }); %>
                <% } else { %>
                    <p style="text-align:center; width:100%;">등록된 프로젝트가 없습니다.</p>
                <% } %>
                </div>
        </section>
        
        <footer style="text-align: center; margin-top: 50px; color: #aaa; font-size: 0.8rem;">
            <p>&copy; 2025 <%= profile ? profile.name : 'User' %>'s Portfolio. All rights reserved.</p>
        </footer>

    </div>

</body>
</html>